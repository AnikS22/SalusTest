\documentclass[border=10pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{shapes,arrows,positioning,fit}

\begin{document}

\begin{tikzpicture}[
    block/.style={rectangle, draw, fill=blue!20, text width=4cm, text centered, rounded corners, minimum height=1.5cm},
    smallblock/.style={rectangle, draw, fill=orange!20, text width=3cm, text centered, rounded corners, minimum height=1cm},
    robot/.style={rectangle, draw, fill=gray!20, text width=2.5cm, text centered, minimum height=3cm},
    alert/.style={rectangle, draw, fill=red!30, text width=3cm, text centered, rounded corners, minimum height=1cm},
    arrow/.style={thick,->,>=stealth}
]

% Robot arm
\node[robot] (robot) {
    \textbf{7-DoF}\\
    \textbf{Robot Arm}\\[0.3cm]
    \begin{tikzpicture}[scale=0.2]
        \fill[gray!40] (0,0) rectangle (1,0.3);
        \fill[blue!30] (0.5,0.3) rectangle (0.7,1.2);
        \fill[blue!30] (0.4,1.2) rectangle (0.8,1.4);
        \fill[red!40] (0.3,1.4) rectangle (0.9,1.6);
        \fill[black] (0.6,0.3) circle (0.08);
        \fill[black] (0.6,1.2) circle (0.08);
    \end{tikzpicture}
};

% Camera
\node[smallblock, above=1.5cm of robot, xshift=-2cm] (camera) {RGB-D\\Camera};

% VLA Model
\node[block, right=3cm of robot] (vla) {
    \textbf{VLA Model}\\
    OpenVLA 7B\\[0.2cm]
    $\mathbf{a}_t = \pi_\theta(\mathbf{o}_t, \ell)$
};

% SALUS Monitor
\node[block, below=2cm of vla] (salus) {
    \textbf{SALUS Monitor}\\
    12D Signal Extraction\\
    Temporal Prediction\\[0.2cm]
    Latency: 100ms
};

% Alert display
\node[alert, left=3cm of salus] (alert) {
    \textbf{ALERT STATUS}\\
    Risk: 0.78\\
    Lead time: 687ms\\
    State: CRITICAL
};

% Control loop
\node[smallblock, below=1.5cm of robot] (control) {
    Intervention\\
    Controller
};

% Task objects
\node[above=2.5cm of robot, xshift=2cm, text width=4cm, align=center] (task) {
    \textbf{Task:} Pick and place\\
    \textbf{Objects:} Mugs, blocks, bottles\\
    \textbf{Scenarios:} Collision, drops, misses
};

% Arrows
\draw[arrow] (camera) -- node[above, sloped] {Image} (vla);
\draw[arrow] (vla) -- node[right] {Action $\mathbf{a}_t$} (robot);
\draw[arrow] (vla) -- node[right, text width=2.5cm, align=center] {Signals\\$\mathbf{z}_t$} (salus);
\draw[arrow] (salus) -- node[above] {$p_\text{fail}$} (alert);
\draw[arrow] (alert) -- node[above, text width=3cm, align=center] {Intervention\\signal} (control);
\draw[arrow] (control) -- node[left] {Modified\\action} (robot);

% Feedback loop
\draw[arrow, dashed] (robot) -- ++(0,-1.5) -| node[near start, below] {State feedback} (vla);

% Timeline examples
\node[draw, dashed, rectangle, below=3cm of salus, text width=12cm, align=left, fill=yellow!10] (examples) {
    \textbf{Deployment Results (50 episodes):}\\[0.2cm]
    \textbf{Episode 12:} Collision predicted 687ms in advance $\rightarrow$ Intervention: Slow mode $\rightarrow$ \textcolor{green!60!black}{\textbf{Success}}\\
    \textbf{Episode 24:} Object drop predicted 512ms in advance $\rightarrow$ Intervention: Freeze+replan $\rightarrow$ \textcolor{green!60!black}{\textbf{Success}}\\
    \textbf{Episode 38:} Task miss predicted 825ms in advance $\rightarrow$ Intervention: Replanning $\rightarrow$ \textcolor{green!60!black}{\textbf{Success}}\\
    \textbf{Episode 47:} Sudden collision (external) $\rightarrow$ No prediction $\rightarrow$ \textcolor{red}{\textbf{Failure}}\\[0.2cm]
    \textbf{Overall:} 87\% intervention success rate, 0.15 false alarms/min
};

\end{tikzpicture}

\end{document}
